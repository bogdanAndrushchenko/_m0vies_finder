import { NavLink } from "react-router-dom";

import s from "./Navigation.module.scss";

const Navigation = () => {
  return (
    <nav>
      <NavLink></NavLink>
    </nav>
  );
};

export default Navigation;
